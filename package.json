{
  "name": "prototype-restarter",
  "version": "1.0.0",
  "description": "Restarts heroku prototypes based on referrer header, so can be used as error page for stopped prototype to restart it automatically",
  "main": "src/app.js",
  "engines": {
    "node": "v18.16.0"
  },
  "scripts": {
    "start": "node -r dotenv/config src/server.js",
    "test": "jest --collectCoverage ts"

  },
  "author": "HMRC Platform UI Team",
  "license": "Apache",
  "dependencies": {
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "heroku-client": "^3.1.0",
    "winston": "^3.9.0"
  },
  "devDependencies": {
    "jest": "^27.3.1",
    "supertest": "^6.3.3",
    "ts-jest": "^27.0.7"
  }
}
